# 실제로 실행될 쿼리 형태를 로그에서 볼 수 있도록 설정
spring.jpa.show-sql=true

# 출력되는 쿼리 로그를 Mysql 버전으로 변경
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

spring.h2.console.enabled=true

server.port=8081